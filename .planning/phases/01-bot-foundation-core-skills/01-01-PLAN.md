---
phase: 01-bot-foundation-core-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - body/bot.js
  - body/interrupt.js
  - body/normalizer.js
autonomous: true
requirements: [BOT-01, BOT-05]

must_haves:
  truths:
    - "Bot connects to Paper 1.21.1 in offline mode and stays connected for 30+ seconds without disconnecting"
    - "Pathfinder and tool plugins are loaded and movements are set on spawn"
    - "bot.interrupt_code is initialized to false on spawn and can be set/cleared by external callers"
  artifacts:
    - path: "body/bot.js"
      provides: "Bot creation, plugin loading, spawn lifecycle, reconnect logic"
      exports: ["createBot"]
    - path: "body/interrupt.js"
      provides: "Interrupt flag management helpers"
      exports: ["clearInterrupt", "isInterrupted"]
    - path: "body/normalizer.js"
      provides: "Item name normalization for v2 body layer"
      exports: ["normalizeItemName"]
    - path: "package.json"
      provides: "mineflayer-pathfinder and mineflayer-tool dependencies"
  key_links:
    - from: "body/bot.js"
      to: "mineflayer"
      via: "createBot({ auth: 'offline', version: '1.21.1' })"
      pattern: "mineflayer\\.createBot"
    - from: "body/bot.js"
      to: "mineflayer-pathfinder"
      via: "bot.loadPlugin(pathfinder)"
      pattern: "loadPlugin.*pathfinder"
    - from: "body/bot.js"
      to: "body/interrupt.js"
      via: "interrupt_code initialization on spawn"
      pattern: "interrupt_code.*=.*false"
---

<objective>
Install Mineflayer plugins, create the bot lifecycle module, interrupt harness, and port the item normalizer to the new body/ directory. This is the foundation every subsequent plan builds on.

Purpose: Establish a connected headless bot with pathfinder + tool plugins loaded, an interrupt system for cooperative cancellation, and item name normalization — the three prerequisites for all skills.
Output: body/bot.js, body/interrupt.js, body/normalizer.js, updated package.json
</objective>

<execution_context>
@/home/bigphoot/.claude/get-shit-done/workflows/execute-plan.md
@/home/bigphoot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bot-foundation-core-skills/01-RESEARCH.md
@agent/normalizer.js
@agent/config.js
@agent/logger.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create bot lifecycle module</name>
  <files>package.json, body/bot.js, body/interrupt.js</files>
  <read_first>
    - package.json (current dependencies)
    - agent/config.js (env var patterns, loadAgentConfig)
    - agent/logger.js (logging export patterns)
    - .planning/phases/01-bot-foundation-core-skills/01-RESEARCH.md (Pattern 5: Bot Connection, Pattern 2: Cooperative Interrupt Check)
  </read_first>
  <action>
    1. Run `npm install mineflayer-pathfinder mineflayer-tool` to add both dependencies to package.json.

    2. Create `body/bot.js` with these exact exports and behavior:
       - `export async function createBot(options = {})` — creates and returns a connected mineflayer bot
       - Options: `{ host, port, username, auth, version }` with defaults from env vars:
         - `host`: `process.env.MC_HOST || 'localhost'`
         - `port`: `parseInt(process.env.MC_PORT || '25565')`
         - `username`: `process.env.MC_USERNAME || 'jeffrey'`
         - `auth`: `'offline'` (hardcoded — Paper server)
         - `version`: `'1.21.1'` (hardcoded — must match Paper server)
       - After `mineflayer.createBot()`, load plugins: `bot.loadPlugin(pathfinder)` and `bot.loadPlugin(toolPlugin)`
       - Return a Promise that resolves with the bot on `'spawn'` event:
         - In spawn handler: `const movements = new Movements(bot); movements.allowSprinting = true; bot.pathfinder.setMovements(movements)`
         - Initialize interrupt: `bot.interrupt_code = false`
       - Register error/disconnect handlers:
         - `bot.on('error', (err) => console.error('[bot] error:', err.message))`
         - `bot.on('kicked', (reason) => console.error('[bot] kicked:', reason))`
         - `bot.on('end', () => console.log('[bot] disconnected'))`
       - If spawn does not fire within 30 seconds, reject the Promise with `new Error('spawn_timeout')`

       Imports:
       ```javascript
       import mineflayer from 'mineflayer'
       import { pathfinder, Movements } from 'mineflayer-pathfinder'
       import { plugin as toolPlugin } from 'mineflayer-tool'
       ```

    3. Create `body/interrupt.js` with these exact exports:
       - `export function clearInterrupt(bot)` — sets `bot.interrupt_code = false`
       - `export function isInterrupted(bot)` — returns `!!bot.interrupt_code`
       - `export function requestInterrupt(bot)` — sets `bot.interrupt_code = true`

    File conventions: ES Modules, 2-space indent, no-semi, single quotes, module header comment, named exports only.
  </action>
  <verify>
    <automated>node -e "import('./body/bot.js').then(m => { console.log('exports:', Object.keys(m)); if (!m.createBot) throw new Error('missing createBot') })" && node -e "import('./body/interrupt.js').then(m => { const keys = Object.keys(m); console.log('exports:', keys); if (!keys.includes('clearInterrupt') || !keys.includes('isInterrupted') || !keys.includes('requestInterrupt')) throw new Error('missing exports') })" && node -e "const pkg = JSON.parse(require('fs').readFileSync('package.json')); if (!pkg.dependencies['mineflayer-pathfinder']) throw new Error('missing pathfinder dep'); if (!pkg.dependencies['mineflayer-tool']) throw new Error('missing tool dep'); console.log('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"mineflayer-pathfinder"` in dependencies
    - package.json contains `"mineflayer-tool"` in dependencies
    - body/bot.js contains `export async function createBot`
    - body/bot.js contains `mineflayer.createBot`
    - body/bot.js contains `bot.loadPlugin(pathfinder)`
    - body/bot.js contains `bot.loadPlugin(toolPlugin)`
    - body/bot.js contains `bot.pathfinder.setMovements(movements)`
    - body/bot.js contains `bot.interrupt_code = false`
    - body/bot.js contains `version: '1.21.1'`
    - body/bot.js contains `auth: 'offline'`
    - body/interrupt.js contains `export function clearInterrupt`
    - body/interrupt.js contains `export function isInterrupted`
    - body/interrupt.js contains `export function requestInterrupt`
    - All three files start with a `// filename — purpose` header comment
  </acceptance_criteria>
  <done>body/bot.js exports createBot that creates an offline-mode mineflayer bot on Paper 1.21.1 with pathfinder and tool plugins loaded on spawn; body/interrupt.js exports cooperative interrupt helpers; mineflayer-pathfinder and mineflayer-tool are in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Port normalizer to body/ and add block name support</name>
  <files>body/normalizer.js</files>
  <read_first>
    - agent/normalizer.js (v1 normalizer implementation — the source to port)
  </read_first>
  <action>
    Copy `agent/normalizer.js` to `body/normalizer.js` with these modifications:

    1. Keep the entire existing `normalizeItemName` function exactly as-is (same ALIASES, same pipeline).

    2. Add a new export `normalizeBlockName(name)` that does the same normalization pipeline but validates against `mcData.blocksByName` instead of `mcData.itemsByName`. This is needed because skills like gather and mine work with block names, not item names. The function should:
       - Use the same Steps 1-9 pipeline as normalizeItemName
       - Replace `mcData.itemsByName` lookups with `mcData.blocksByName`
       - Share the same ALIASES map (most aliases work for both items and blocks)

    3. To avoid code duplication, extract the shared pipeline into a private helper:
       ```javascript
       function _normalize(name, registry) {
         // Steps 1-9 using `registry` for lookups instead of hardcoded itemsByName
       }
       export function normalizeItemName(name) { return _normalize(name, mcData.itemsByName) }
       export function normalizeBlockName(name) { return _normalize(name, mcData.blocksByName) }
       ```

    4. Keep the same module header style: `// normalizer.js — Canonicalize LLM item/block names to MC 1.21.1 registry IDs`
  </action>
  <verify>
    <automated>node -e "import('./body/normalizer.js').then(m => { console.log('exports:', Object.keys(m)); if (m.normalizeItemName('sticks') !== 'stick') throw new Error('item alias failed'); if (m.normalizeItemName('cobble') !== 'cobblestone') throw new Error('item alias 2 failed'); if (!m.normalizeBlockName) throw new Error('missing normalizeBlockName'); if (m.normalizeBlockName('cobble') !== 'cobblestone') throw new Error('block alias failed'); console.log('all normalizer tests pass') })"</automated>
  </verify>
  <acceptance_criteria>
    - body/normalizer.js exists and starts with `// normalizer.js`
    - body/normalizer.js contains `export function normalizeItemName`
    - body/normalizer.js contains `export function normalizeBlockName`
    - body/normalizer.js contains the ALIASES map with at least `'sticks': 'stick'` and `'cobble': 'cobblestone'`
    - body/normalizer.js imports `minecraft-data` and initializes with `'1.21.1'`
    - `normalizeItemName('sticks')` returns `'stick'`
    - `normalizeBlockName('cobble')` returns `'cobblestone'`
  </acceptance_criteria>
  <done>body/normalizer.js exports both normalizeItemName and normalizeBlockName, ported from v1 with block registry support added</done>
</task>

</tasks>

<verification>
1. `node -e "import('./body/bot.js')"` — imports without error
2. `node -e "import('./body/interrupt.js')"` — imports without error
3. `node -e "import('./body/normalizer.js').then(m => console.log(m.normalizeItemName('wooden_planks')))"` — prints `oak_planks`
4. `grep mineflayer-pathfinder package.json` — dependency present
5. `grep mineflayer-tool package.json` — dependency present
</verification>

<success_criteria>
- body/ directory exists with bot.js, interrupt.js, normalizer.js
- Bot can be created programmatically with `createBot()` (tested in Plan 02 against live server)
- Interrupt flag management works via exported helpers
- Item and block name normalization works for all v1 aliases plus new block registry
- mineflayer-pathfinder and mineflayer-tool installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-bot-foundation-core-skills/01-01-SUMMARY.md`
</output>
