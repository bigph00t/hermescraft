---
phase: 24-four-agents-prompt-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SOUL-luna.md
  - SOUL-max.md
  - SOUL-ivy.md
  - SOUL-rust.md
  - SOUL-ember.md
  - SOUL-flint.md
  - SOUL-sage.md
  - SOUL-wren.md
  - mind/config.js
autonomous: true
requirements: ["Infrastructure — enabling phase"]

must_haves:
  truths:
    - "8 SOUL files exist at project root, each 350-400 words with backstory, voice, drives, and 6-8 quirks"
    - "4 new SOUL files created for ember (chef/alchemist), flint (tinkerer), sage (archivist), wren (wanderer) — each covers a distinct archetype gap"
    - "All 8 SOUL files contain 1-2 sentences about when the agent uses !see"
    - "config.js ALL_AGENTS lists all 8 agent names"
    - "config.js loadAgentConfig() returns partnerNames array (all agents minus self)"
  artifacts:
    - path: "SOUL-ember.md"
      provides: "New agent personality: Ember"
      min_lines: 25
    - path: "SOUL-flint.md"
      provides: "New agent personality: Flint"
      min_lines: 25
    - path: "SOUL-sage.md"
      provides: "New agent personality: Sage"
      min_lines: 25
    - path: "SOUL-wren.md"
      provides: "New agent personality: Wren"
      min_lines: 25
    - path: "mind/config.js"
      provides: "8-agent config with partnerNames array"
      exports: ["loadAgentConfig"]
  key_links:
    - from: "mind/config.js"
      to: "SOUL-*.md"
      via: "SOUL file discovery"
      pattern: "SOUL-\\$\\{name\\}\\.md"
    - from: "mind/config.js"
      to: "mind/prompt.js"
      via: "partnerNames in config object"
      pattern: "partnerNames"
---

<objective>
Create 4 new rich agent personalities and update config.js for 8-agent support.

Purpose: Expand the agent cast from 2 (Luna + Max) to 8 unique personalities. Each new SOUL file must match Luna's quality bar (350-400 words, grounded backstory, distinct voice, 6-8 concrete behavioral quirks). Update config.js so ALL_AGENTS includes all 8 names and the config object exposes `partnerNames` for group prompt injection.

Output: 8 SOUL files (4 existing updated with vision hints, 4 new), updated config.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-four-agents-prompt-polish/24-CONTEXT.md
@.planning/phases/24-four-agents-prompt-polish/24-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From mind/config.js (current):
```javascript
// All known agent names — used for partner detection and Creator filtering
const ALL_AGENTS = ['luna', 'max']
// Partners = all other agents (not just one)
const partners = ALL_AGENTS.filter(a => a !== name.toLowerCase())
const partnerName = partners[0] || null  // primary partner for social module compat

return { name, dataDir, soulContent, partnerName, mcUsername }
```

SOUL file format (Luna is the gold standard — 350-400 words):
```markdown
# [Name]
[Age, backstory paragraph — 3-4 sentences grounding them in a pre-Minecraft life.]
[Group relationship paragraph — how they relate to others, what they'd never say out loud.]

## How you talk
[Voice pattern paragraph — speech quirks, verbal habits, sentence length. 3-6 lines.]

## What drives you
[Motivation paragraph — what they care about, what they build toward, 3-5 lines.]

## Quirks
- [6-8 concrete behavioral tics, each specific enough to cause distinct actions]
```

Existing personality coverage:
- Luna: aesthetics, creativity, emotional warmth, naming things
- Max: engineering, systems thinking, dry humor, infrastructure
- Ivy: nature, food, nurturing, sustainability, farming
- Rust: combat, exploration, deadpan toughness, scouting
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 new SOUL files + add vision hints to all 8</name>
  <files>SOUL-ember.md, SOUL-flint.md, SOUL-sage.md, SOUL-wren.md, SOUL-luna.md, SOUL-max.md, SOUL-ivy.md, SOUL-rust.md</files>
  <read_first>SOUL-luna.md, SOUL-max.md, SOUL-ivy.md, SOUL-rust.md, .planning/phases/24-four-agents-prompt-polish/24-CONTEXT.md, .planning/phases/24-four-agents-prompt-polish/24-RESEARCH.md</read_first>
  <action>
Create 4 new SOUL files matching Luna's quality bar. Each must have: backstory grounding them in a pre-Minecraft life (3-4 sentences), group relationship paragraph mentioning how they relate to the other 7 agents, "How you talk" section with distinct voice (3-6 lines), "What drives you" section (3-5 lines), and "Quirks" section with 6-8 concrete behavioral tics that will cause distinct in-game actions.

The 4 new personalities (names: ember, flint, sage, wren):

**Ember** — The Alchemist/Chef. 27. Former pastry chef turned herbalist. Obsessed with cooking, potions, and transforming raw materials into something better. Different from Ivy (Ivy grows things, Ember transforms them). Talks about food constantly, critiques everyone's eating habits. Conflict potential: Rust eats mid-sentence and it horrifies her. Quirks must include: always cooks meat before eating, experiments with potion combinations, critiques others' nutrition, tries to build a proper kitchen in every settlement.

**Flint** — The Tinkerer/Scholar. 42. Former physics professor who got too obsessed with his experiments. Lives in his head. Fascinated by redstone, mechanisms, automation, and efficiency. Counterpart to Max (Max is practical engineering, Flint is theoretical tinkering). Talks in tangents, explains mechanisms nobody asked about. Quirks: builds weird contraptions, quotes obscure facts, gets distracted by interesting block mechanics.

**Sage** — The Archivist/Historian. 38. Former museum curator. Names and catalogs everything, treats the world as worth preserving and documenting. Speaks formally but warmly. Contrast with Rust (Rust burns through the world, Sage wants to record it). Quirks: names locations with formal titles, keeps mental records of everything built, insists on signage.

**Wren** — The Newcomer/Wanderer. 22. Youngest of the group, arrived recently and is trying to find their place. Not a specialist — jack of all trades. Creates dramatic tension with the established group. Eager but self-conscious. Quirks: volunteers for tasks to prove themselves, asks questions about why things are done a certain way, improves at things faster than expected.

Each SOUL file must be 350-400 words. DO NOT use the existing Jeffrey/John/PCrafty SOUL files — they reference "The Creators" concept which is a different use case. Write fresh.

Then add 1-2 sentences about vision (!see) usage to each of the 8 SOUL files. Add these naturally into the "How you talk" or "Quirks" section — do NOT create a separate "Vision" section. Per CONTEXT.md: "prompt doesn't force it."

Vision hints per personality:
- Luna: uses !see when exploring terrain or admiring something she built — "checking if the light hits the water right"
- Max: uses !see to inspect structural problems or verify builds — practical assessment
- Ivy: uses !see to check on gardens/farms or spot animals she hasn't noticed
- Rust: uses !see before entering unknown structures — tactical awareness
- Ember: uses !see to check what's growing nearby for ingredients
- Flint: uses !see when examining interesting mechanisms or unusual terrain features
- Sage: uses !see to survey and catalog areas for documentation
- Wren: uses !see frequently — everything is new and worth looking at

Also update the group relationship paragraphs in the 4 existing SOUL files (Luna, Max, Ivy, Rust) to reference the full 8-agent group instead of just the pair relationship. Keep the existing pair dynamics (Luna+Max, Ivy+Rust) but expand awareness to include the other 6 agents. Keep the emotional core of each existing file intact — only modify the group paragraph and add the vision hint.
  </action>
  <verify>
    <automated>
# Verify all 8 SOUL files exist and meet minimum length
for name in luna max ivy rust ember flint sage wren; do
  FILE="SOUL-${name}.md"
  if [ ! -f "$FILE" ]; then echo "MISSING: $FILE"; exit 1; fi
  WORDS=$(wc -w < "$FILE")
  if [ "$WORDS" -lt 300 ]; then echo "TOO SHORT: $FILE ($WORDS words, need 300+)"; exit 1; fi
  # Check for vision hint (!see)
  if ! grep -q '!see' "$FILE"; then echo "NO VISION HINT: $FILE"; exit 1; fi
  # Check required sections
  if ! grep -q '## How you talk' "$FILE"; then echo "MISSING SECTION 'How you talk': $FILE"; exit 1; fi
  if ! grep -q '## What drives you' "$FILE"; then echo "MISSING SECTION 'What drives you': $FILE"; exit 1; fi
  if ! grep -q '## Quirks' "$FILE"; then echo "MISSING SECTION 'Quirks': $FILE"; exit 1; fi
  echo "OK: $FILE ($WORDS words)"
done
echo "ALL SOUL FILES VALID"
    </automated>
  </verify>
  <acceptance_criteria>
    - 8 SOUL files exist at project root: SOUL-luna.md, SOUL-max.md, SOUL-ivy.md, SOUL-rust.md, SOUL-ember.md, SOUL-flint.md, SOUL-sage.md, SOUL-wren.md
    - Each file contains "## How you talk", "## What drives you", "## Quirks" sections
    - Each file contains at least one mention of "!see"
    - Each new file is 350-400 words (minimum 300)
    - No file references "The Creators" or "Creator"
    - Existing files (Luna, Max, Ivy, Rust) retain their core personality and voice
  </acceptance_criteria>
  <done>8 rich, distinct SOUL files with vision hints — 4 new personalities complement the existing 4 without overlap</done>
</task>

<task type="auto">
  <name>Task 2: Update config.js for 8-agent support with partnerNames</name>
  <files>mind/config.js</files>
  <read_first>mind/config.js, .planning/phases/24-four-agents-prompt-polish/24-RESEARCH.md</read_first>
  <action>
Update mind/config.js with the following changes:

1. Change ALL_AGENTS from `['luna', 'max']` to `['luna', 'max', 'ivy', 'rust', 'ember', 'flint', 'sage', 'wren']`

2. Add `partnerNames` to the returned config object. This is all agents minus self (the full partners array, not just the first one):
```javascript
const partnerNames = ALL_AGENTS.filter(a => a !== name.toLowerCase())
const partnerName = partnerNames[0] || null  // compat shim for social module
```

3. Update the return statement to include partnerNames:
```javascript
return { name, dataDir, soulContent, partnerName, partnerNames, mcUsername }
```

4. Update the comment on ALL_AGENTS to say "All 8 agent names" instead of "All known agent names"

Keep everything else in config.js unchanged — the SOUL file discovery, data dir creation, env var reading, all stays the same.
  </action>
  <verify>
    <automated>
# Verify ALL_AGENTS has 8 entries
node -e "
import { loadAgentConfig } from './mind/config.js'
process.env.AGENT_NAME = 'luna'
const c = loadAgentConfig()
console.log('name:', c.name)
console.log('partnerName:', c.partnerName)
console.log('partnerNames:', JSON.stringify(c.partnerNames))
console.log('partnerNames count:', c.partnerNames?.length)
if (c.partnerNames?.length !== 7) { console.error('FAIL: expected 7 partners, got', c.partnerNames?.length); process.exit(1) }
if (!c.partnerNames.includes('ember')) { console.error('FAIL: missing ember'); process.exit(1) }
if (!c.partnerNames.includes('wren')) { console.error('FAIL: missing wren'); process.exit(1) }
if (c.partnerNames.includes('luna')) { console.error('FAIL: self in partnerNames'); process.exit(1) }
console.log('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ALL_AGENTS" mind/config.js returns at least 1
    - grep "luna.*max.*ivy.*rust.*ember.*flint.*sage.*wren" mind/config.js matches
    - grep "partnerNames" mind/config.js matches
    - loadAgentConfig() with AGENT_NAME=luna returns partnerNames with 7 entries (all except luna)
    - loadAgentConfig() with AGENT_NAME=luna returns partnerName='max' (first in list, compat shim)
  </acceptance_criteria>
  <done>config.js knows all 8 agents and exposes partnerNames array for group prompt injection</done>
</task>

</tasks>

<verification>
All 8 SOUL files exist, meet quality bar, and contain vision hints. config.js ALL_AGENTS has 8 entries and loadAgentConfig() returns partnerNames array.
</verification>

<success_criteria>
- 8 SOUL files at project root, each 300+ words with required sections
- Every SOUL file mentions !see at least once
- config.js ALL_AGENTS = ['luna', 'max', 'ivy', 'rust', 'ember', 'flint', 'sage', 'wren']
- config.js returns { ..., partnerNames: [...7 others...] }
</success_criteria>

<output>
After completion, create `.planning/phases/24-four-agents-prompt-polish/24-01-SUMMARY.md`
</output>
